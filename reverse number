#include <stdio.h>

int
main ()
{
  int num,x ;
  printf ("Enter the number:");
  scanf ("%d", &num);

  
  while (num > 0)
    { x = num % 10;
      printf ("%d", x);
      num /= 10;
}

  return 0;
}
